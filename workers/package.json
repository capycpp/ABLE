{
  "name": "able-math-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outdir=../dist --platform=neutral --target=es2020 --format=esm",
    "dev": "echo 'Use Wrangler or Pages for local dev'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260902.1",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}
